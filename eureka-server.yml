
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_SERVER_URL}
management:
  endpoints:
    web:
      exposure:
        include: health, metrics, env
  endpoint:
    health:
      show-details: always
    env:
      enabled: true
      show-values: always
